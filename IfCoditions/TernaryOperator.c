#include <stdio.h>

int main (){
    
 // (condiction) ? ( si la condiction est vrais) : ( si la condiction est faux);
    // int age = 18;
    // (age >= 18) ? printf("vous etez eligible a vote"): printf("vous etez pas eligible a vote");

// odd & even 
    int number = 11;

    (number % 2 == 0) ? printf("la nombre est pair")
     : printf("la nombre est inpair");
    return 0;
}  